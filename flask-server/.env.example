# Example .env for flask-server
# Copy this file to `.env` and fill in your real values locally (do NOT commit `.env`).

MONGO_URI="your-mongodb-connection-string-here"
SECRET_KEY="your-flask-secret-key-here"
